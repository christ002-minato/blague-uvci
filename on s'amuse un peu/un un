/* ========== RESET SIMPLE ========== */
*,
*::before,
*::after {
  box-sizing: border-box;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="test.css">
    <title>Document</title>
</head>
<body>
 <header>
    <nav>
        
        <div>
            
            <ul>
                <div class="logo">
                <a href="#"><img src="Logo.png" alt="Logo"></a>
            </div>
                <li><a href="#">HOME</a></li>
                <li><a href="#">SHOP</a></li>
                <li><a href="#">MAGAZINE</a></li>
            </ul>
        </div>
        <div>
            <ul>
                <li><a href="#"><img src="Cart Button.png" alt="Cart Button"></a></li>
                <li><a href="#">LOGIN</a></li>
            </ul>
        </div>
    </nav>
    <section class="search-bar">
        <img src="Search.png" alt="recherche">
        <input type="text" placeholder="living room">
        <span class="powered">Powered by Algolia</span>
        <img src="Sajari Logo.png" alt="">
    </section>
 </header>
 
</body>
</html>
html {
  font-size: 16px;
}

body {
  margin: 0;
  font-family: 'Roboto', sans-serif;
  color: #111;
}

/* Accessible: cacher visuellement mais laisser aux lecteurs d’écran */
.visually-hidden {
  position: absolute !important;
  width: 1px !important;
  height: 1px !important;
  padding: 0 !important;
  margin: -1px !important;
  overflow: hidden !important;
  clip: rect(0, 0, 0, 0) !important;
  border: 0 !important;
}

/* ========== 1) HEADER ========== */
/* On aligne les éléments avec FLEXBOX */
.header {
  display: flex;                 /* Active Flexbox */
  align-items: center;           /* Aligne verticalement */
  justify-content: space-between;/* Espace entre gauche et droite */
  padding: 16px 24px;
  border-bottom: 1px solid #e5e5e5;
  gap: 16px;                     /* espace entre éléments flex */
}

.burger {
  font-size: 1.4rem;
  background: none;
  border: 0;
  cursor: pointer;
}

.logo {
  font-weight: 700;
  text-decoration: none;
  color: #111;
}

/* Menu principal */
.main-nav .nav-list {
  display: flex;                  /* Alignement en ligne */
  gap: 24px;                      /* Espace uniforme entre liens */
  list-style: none;
  margin: 0;
  padding: 0;
}

.main-nav .nav-list a {
  text-decoration: none;
  color: #111;
  font-weight: 500;
  letter-spacing: .05em;
}

.header-right {
  display: flex;
  gap: 16px;                      /* Espace entre Login et panier */
  align-items: center;
}

.header-right a {
  text-decoration: none;
  color: #111;
}

/* ========== 2) TOPBAR : Recherche + Best match ========== */
.topbar {
  display: flex;
  justify-content: space-between; /* Bien respecter l’espace entre la barre et “Best match” */
  align-items: center;
  padding: 24px;                  /* Espace autour */
  border-bottom: 1px solid #e5e5e5;
  gap: 24px;
}

.search {
  position: relative;
  flex: 1;                        /* prend toute la largeur dispo */
}

.search-icon {
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
}

.search input {
  width: 100%;
  padding: 8px 0 8px 28px;        /* laisse la place à l’icone */
  font-size: 1.1rem;
  border: 0;
  border-bottom: 2px solid #111;
  outline: none;
}

.powered {
  position: absolute;
  right: 32px;
  bottom: -20px;
  font-size: 0.8rem;
  color: #777;
}

.settings {
  position: absolute;
  right: 0;
  top: 50%;
  transform: translateY(-50%);
  background: none;
  border: 0;
  cursor: pointer;
  font-size: 1.1rem;
}

.sort {
  display: flex;
  align-items: center;
  gap: 8px;
}

.sort label {
  font-weight: 500;
}

/* ========== 3) LAYOUT PRINCIPAL ========== */
.layout {
  display: grid;
  grid-template-columns: 260px 1fr;   /* sidebar fixe + contenu fluide */
  gap: 32px;                          /* espace entre filtres et produits */
  padding: 24px;
}

/* ========== 4) FILTRES (sidebar) ========== */
.filters-title {
  font-size: 0.75rem;
  letter-spacing: .15em;
  color: #777;
  margin-bottom: 16px;
}

.filter {
  border-bottom: 1px solid #eee;
  padding: 12px 0;
}

.filter summary {
  cursor: pointer;
  list-style: none;
  font-weight: 500;
}

.filter-list {
  list-style: none;
  padding-left: 0;
  margin: 8px 0 0 0;
  display: grid;
  gap: 6px;
}

.price-range {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}

/* ========== 5) PRODUITS (Grid) ========== */
/* Ici on utilise CSS GRID pour aligner les cartes produits */
.products {
  display: grid;
  grid-template-columns: repeat(3, 1fr); /* 3 colonnes comme sur la maquette */
  gap: 32px;                              /* espace entre les cartes */
}

.card {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.thumb {
  aspect-ratio: 4 / 3;                    /* garde un ratio régulier pour les images */
  background: #f5f5f5;                    /* placeholder si pas d'image */
  overflow: hidden;
}

.thumb img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.card-info {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.title-price {
  display: flex;
  justify-content: space-between;         /* Nom à gauche, prix à droite */
  align-items: center;
}

.title {
  margin: 0;
  font-size: 1rem;
  font-weight: 600;
}

.price {
  font-weight: 500;
}

.category {
  text-transform: uppercase;
  font-size: 0.75rem;
  color: #777;
  letter-spacing: .08em;
  margin: 0 0 6px;
}

.meta {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.stars {
  letter-spacing: 2px;                    /* espace entre les étoiles */
  font-size: 0.9rem;
  color: #111;
}

.add-to-cart {
  background: none;
  border: 0;
  cursor: pointer;
  font-size: 1.1rem;
}

/* ========== 6) PAGINATION UTILISABLE ========== */
.pagination {
  display: flex;
  justify-content: center;
  padding: 32px 0 64px;
}

.pagination ul {
  list-style: none;
  display: flex;
  gap: 8px;
  padding: 0;
  margin: 0;
  align-items: center;
}

.pagination a,
.pagination span {
  display: inline-block;
  padding: 6px 10px;
  text-decoration: none;
  color: #111;
  border-radius: 2px;
}

.pagination a:hover {
  background: #eee;
}

.pagination a.active {
  background: #111;
  color: #fff;
}

/* ========== 7) RESPONSIVE (mobile / tablette) ========== */
@media (max-width: 1024px) {
  .products {
    grid-template-columns: repeat(2, 1fr); /* 2 colonnes */
  }
}

@media (max-width: 768px) {
  .header {
    flex-wrap: wrap;
  }

  .main-nav {
    order: 3;
    width: 100%;
  }

  .main-nav .nav-list {
    justify-content: center;
  }

  .topbar {
    flex-direction: column;
    align-items: stretch;
  }

  .layout {
    grid-template-columns: 1fr;        /* les filtres passent au-dessus de la grille */
  }

  .products {
    grid-template-columns: 1fr;        /* 1 colonne sur mobile */
  }
}
